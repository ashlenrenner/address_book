<!DOCTYPE html>
<html>
    <head>
        <title>Address Book</title>

        <!-- css -->
    <link href="/css/main.css" rel="stylesheet" type="text/css">
    
    </head>
    <body>
        <h3>Contcats</h3>
        <% if (users.length) { %>
        <ul>
            <% for (var i in users) { %>
            <li><a href="/users/<%= users[i].username %>"><%= users[i].name%> <%=users[i].username %></a><br><small><%= users[i].title%></small></li>
            <% } %>
        </ul>
        <% } else { %>
        <p>Sorry, there are no contacts yet.</p>
        <% } %>

        <p class="new"><a href="/user/new">New Contact</a></p>



    </body>
</html>
